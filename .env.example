# Frontend environment variables
# If left unset, frontend will proxy to /api (Vite proxy to http://localhost:5174)
# VITE_API_BASE_URL=http://localhost:5174/api

# Supabase (optional). If unset, the app will use the local MySQL API server.
# Set this to true only if you want to use Supabase.
VITE_USE_SUPABASE=false
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
